import 'package:flutter/material.dart';

class AppRadiuses {
  const AppRadiuses._();

    static const BorderRadius a4 = BorderRadius.all(Radius.circular(4));
    static const BorderRadius a8 = BorderRadius.all(Radius.circular(8));
    static const BorderRadius a10 = BorderRadius.all(Radius.circular(10));
    static const BorderRadius a12 = BorderRadius.all(Radius.circular(12));
    static const BorderRadius a24 = BorderRadius.all(Radius.circular(24));
}
